{
  "name": "student-grading-system",
  "version": "1.0.0",
  "description": "Automated grading scripts for full-stack projects",
  "main": "generate-report.js",
  "scripts": {
    "test": "jest",
    "grade": "node generate-report.js",
    "build": "cd grading-folder/frontend && npm install && npm run build",
    "install:all": "npm install && cd grading-scripts && npm install && cd ../grading-folder/frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "openai": "^4.20.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "@testing-library/react": "^14.1.0",
    "@testing-library/jest-dom": "^6.1.4",
    "playwright": "^1.40.0",
    "mongoose": "^8.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "express-validator": "^7.0.1",
    "socket.io": "^4.6.0",
    "nodemailer": "^6.9.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}